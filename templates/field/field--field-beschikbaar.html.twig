{#
/**
 * @file
 * Theme override for field--field-beschikbaar.html.twig
 * if 'field_beschikbaar' == 'Bezet' change color to red
 * |striptags - the striptags filter strips SGML/XML tags and replace adjacent whitespace by one space
 * |render - https://www.drupal.org/node/2448219
 * {{ item.content|render|striptags }}
 */
#}

{% for item in items %}
  <div{{ item.attributes.addClass('field__disposable') }}>
    {% set strippedText = item.content|render|striptags %}
    {% if strippedText == 'Bezet' %}
      <div class="field__occupied">
        {{ strippedText }}
      </div>
    {% else %}
      <div class="field__free">
        {{ strippedText }}
      </div>
    {% endif %}
  </div>
{% endfor %}